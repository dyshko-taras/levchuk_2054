import 'package:flutter/material.dart';

class MatchCenterPage extends StatelessWidget {
  const MatchCenterPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: SizedBox.shrink());
}
