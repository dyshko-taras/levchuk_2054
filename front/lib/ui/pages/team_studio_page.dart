import 'package:flutter/material.dart';

class TeamStudioPage extends StatelessWidget {
  const TeamStudioPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: SizedBox.shrink());
}
