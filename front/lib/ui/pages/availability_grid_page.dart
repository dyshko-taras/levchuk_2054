import 'package:flutter/material.dart';

class AvailabilityGridPage extends StatelessWidget {
  const AvailabilityGridPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: SizedBox.shrink());
}
