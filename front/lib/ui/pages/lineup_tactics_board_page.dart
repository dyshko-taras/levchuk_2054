import 'package:flutter/material.dart';

class LineupTacticsBoardPage extends StatelessWidget {
  const LineupTacticsBoardPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: SizedBox.shrink());
}
