**FieldCaptain**
**Слоган:** Neighborhood football, organized.

**Призначення:**
Офлайн-органайзер для аматорських футбольних матчів. Дозволяє керувати командами, гравцями, полями, розкладом матчів, складами, тактикою та організаційними деталями. Реєстрація не потрібна. Усі дані зберігаються локально на пристрої.

---

## 1. Splash Screen

### Призначення

Короткий стартовий екран для ініціалізації локального сховища та підготовки застосунку до роботи.

### UI

* **Фон:** повноекранна фонова картинка (футбольний стадіон, темна атмосфера).
* **Затемнення:** легкий темний оверлей для читабельності тексту.
* **Логотип / Назва:**

  * Текст: `FieldCaptain`
  * Розташування: по центру екрану.
* **Сабтайтл:**

  * Текст: `Plan. Play. Repeat.`
  * Розташування: під назвою, меншим шрифтом.
* **Loader:**

  * Тип: індетермінований (circular loader).
  * Розташування: нижня частина екрану, по центру.
* **Інтеракції:** відсутні.

### Логіка

* При відкритті екрану запускається:

  * ініціалізація локальної БД / сховища;
  * перевірка факту першого запуску.
* Splash Screen показується **мінімум 2 секунди**, навіть якщо ініціалізація завершилась швидше.
* Після завершення ініціалізації та мінімального часу:

  * **перший запуск** → перехід на **Onboarding**;
  * **повторний запуск** → перехід на **Hub**.

### Обмеження

* Без кнопок, жестів або навігації користувача.
* Без додаткового тексту або статусів (тільки те, що на скріні).

---

Добре, екран зрозумілий 👍
Коротко зафіксую **Onboarding** без змін логіки, строго за скрінами та описом.

---

## 2. Onboarding

### Призначення

Пояснити цінність застосунку та задати базові початкові налаштування без реєстрації.

### Загальна структура

* Онбординг складається з **3 інформаційних слайдів** та **фінального mini-setup екрану**.
* Фон — повноекранне зображення стадіону з затемненням.
* Навігація:

  * кнопка **Next**
  * кнопка **Skip** (пропускає онбординг і веде одразу до mini-setup)
  * індикатор прогресу (dots)

---

### Slide 1

**Заголовок:**
Build squads fast

**Опис:**
Create teams, add players, keep positions.

**Контент:**

* Превʼю екрану Team Studio
* Кнопка: **Next**

---

### Slide 2

**Заголовок:**
Manage local fields

**Опис:**
Save the pitches you use and reuse them.

**Контент:**

* Превʼю реєстру полів
* Кнопка: **Next**
* Кнопка: **Back**

---

### Slide 3

**Заголовок:**
Schedule smarter

**Опис:**
Plan matches, store results, prepare lineups.

**Контент:**

* Превʼю Availability / календаря
* **Start** (primary)
* **Privacy** (secondary)
* Кнопка: **Back**

#### Логіка

* **Start**

  * зберігає обрані налаштування;
  * створює базові локальні довідники (якщо потрібно);
  * переводить користувача на **Hub**.
* **Privacy**

  * відкриває модальне вікно з текстом політики приватності;
  * закриття модалки повертає на mini-setup.
* **Skip**

  * пропускає слайди;

### Обмеження

* Реєстрація відсутня.
* Усі налаштування зберігаються локально на пристрої.

---

## 3. Hub (Головний екран)

### Призначення

Головний робочий екран застосунку: показує поточний стан справ і надає швидкий доступ до основних дій.

---

### UI

#### Top App Bar

* **Title:** `FieldCaptain`
* **Actions:**

  * ⚙ **Settings**
  * ⋯ **More** (опціонально, зарезервовано)

---

#### Quick Bar (горизонтальна)

Кнопки швидкої навігації:

* **+ Match**
* **My Squad**
* **Fields**
* **Stats**
* **Availability**

Активна кнопка візуально підсвічується.

---

#### Next Match card

* Заголовок: **Next Match**
* Вміст (якщо матч існує):

  * Date & Time
  * Field
  * Status (chip)
* **CTA:** `Open` → перехід до **Match Center**
* Візуал: картка з фоновим зображенням стадіону

**Empty state:**

* Текст: `No match planned.`
* Кнопка: `+ Match`

---

#### My Squad card

* Вміст:

  * Назва команди
  * `Players: N`
* **CTA кнопки:**

  * `Open` → **Team Studio**
  * `Lineup` → **Lineup & Tactics**

    * доступна **тільки якщо є найближчий upcoming матч**

---

#### Fields Snapshot

* Відображає **1–2 останні використані поля**
* Картка поля:

  * Назва
  * Тип (наприклад: Grass · 7v7)
  * Превʼю зображення
* **CTA:** `All Fields` → **Fields Registry**

---

#### Floating Action Button (FAB)

* Іконка: `+`
* Дія: швидке створення матчу → **Match Composer**
* Prefill:

  * Default team (якщо задано)
  * Default field (якщо задано)

---

### Логіка та переходи

* **+ Match (Quick Bar) / FAB** → `Match Composer`
* **My Squad** → `Team Studio`
* **Fields / All Fields** → `Fields Registry`
* **Stats** → `Stats`
* **Availability** → `Availability Grid`
* **Next Match → Open** → `Match Center`

**Кнопка Lineup:**

* Якщо немає upcoming матчу → toast:

  * `No upcoming match.`

---

### Обмеження

* Дані беруться з локального сховища.
* Якщо немає команд / полів / матчів — показуються відповідні empty state.
* Hub не містить створення сутностей напряму (окрім швидкого переходу).

---

## 4. Team Studio (Profile | Roster)

### Призначення

Екран управління командою: налаштування профілю команди та редагування складу гравців.

---

## Загальна структура

* Екран містить **дві вкладки**:

  * **Profile**
  * **Roster**
* Верхня панель:

  * кнопка **Back**
  * кнопка **More (⋯)** з додатковими діями

---

## Tab: Profile

### UI елементи

#### Team name

* Текстове поле
* Обовʼязкове (`*`)
* Назва команди має бути унікальною

#### Badge icon

* Вибір емблеми команди (emoji / іконка)
* Можна обрати одну іконку

#### Home colors

* Превʼю футбольної форми (SVG, front view)
* Вибір кольорів:

  * **Primary color**
  * **Secondary color**
* Кольори обираються через набір **preset-кругів** з пакетом `flutter_colorpicker`
* При виборі кольору:

  * SVG форма оновлюється в реальному часі
  * кожна зона SVG фарбується окремо (SVG складається з 2 paths - base / details)

#### Away colors

* Аналогічно Home colors
* Використовується **та сама SVG-форма**
* Візуальна різниця досягається виключно за рахунок кольорів

#### Кнопка

* **Save** — основна дія екрану

---

### Логіка (Profile)

* **Save**

  * перевіряє, що `Team name` заповнене;
  * перевіряє унікальність назви;
  * зберігає дані команди в локальну БД:

    * name
    * badge
    * homeKit (primaryColor, secondaryColor, templateId)
    * awayKit (primaryColor, secondaryColor, templateId);
  * `templateId` для Home та Away у MVP завжди однаковий;
  * після збереження повертає на попередній екран.

---

## Tab: Roster

### UI елементи

#### Список гравців

Кожен рядок містить:

* **Name**
* **Position** (GK / DF / MF / FW)
* **Number** (`#`)
* Дії:

  * **Edit**
  * **Remove**

#### Додавання гравця

* Кнопка **+ Add Player**
* Відкриває форму створення гравця

---

### Логіка (Roster)

* **Add Player**

  * створює нового гравця та додає до складу
* **Edit Player**

  * редагує дані існуючого гравця
* **Remove Player**

  * видаляє гравця з команди
* Зміни зберігаються локально одразу після дії

---

## Menu: More (⋯)

### Дії

#### Set as default

* Встановлює команду як команду за замовчуванням
* Зберігає `Settings.default_team_id`

#### Delete team

* Видалення дозволене **лише якщо команда не використовується** у:

  * Scheduled матчах
  * Finished матчах
* Якщо команда використовується — дія недоступна або показується повідомлення про заборону

---

## Дані (MVP)

```json
team {
  id,
  name,
  badgeId,
  homeKit: {
    templateId,
    primaryColor,
    secondaryColor
  },
  awayKit: {
    templateId,
    primaryColor,
    secondaryColor
  },
  players: []
}
```

---

## Обмеження та примітки

* Реєстрація відсутня
* Усі дані зберігаються локально
* Home та Away використовують **один і той самий SVG-шаблон**
* Архітектура передбачає додавання нових шаблонів форми у майбутніх версіях без зміни UI

---

Добре, продовжуємо 👍
Нижче — **фінальний technical_spec для 5) Teams Directory**, у тому ж стилі, що й попередні екрани.

---

## 5. Teams Directory

### Призначення

Каталог усіх створених команд. Дозволяє швидко переглянути список команд, відкрити команду для редагування або створити нову.

---

## UI

### Header

* Заголовок: **Teams Directory**
* Кнопки:

  * **Back**
  * **+** (New Team)

---

### Список команд

Команди відображаються у вигляді вертикального списку карток.

#### Картка команди містить:

* **Badge** (іконка / емблема команди)
* **Name** — назва команди
* **Players: N** — кількість гравців у складі
* **Open** — кнопка відкриття команди
* **More (⋯)** — додаткові дії

---

## Дії користувача

### + New Team

* Натискання відкриває **Team Studio** у режимі створення нової команди (Profile → порожні поля).

### Open

* Відкриває **Team Studio** для вибраної команди у режимі редагування.

### More (⋯)

* Поведінка відповідає меню в **Team Studio**:

  * Set as default
  * Delete team (з урахуванням обмежень)

---

## Логіка

* Список формується з локальної БД.
* Дані оновлюються після:

  * створення команди
  * редагування команди
  * видалення команди
* Порядок відображення:

  * за датою створення або алфавітом (без складної логіки для MVP).

---

## Empty state

Якщо команд немає:

* Текст: `No teams created yet.`
* Кнопка: **+ New Team**

---

## Обмеження та примітки

* Реєстрація відсутня.
* Усі дії виконуються локально.
* Teams Directory не містить редагування даних напряму — тільки навігацію до Team Studio.

---
Зафіксували 👍
Тоді одразу даю **оновлену фінальну версію 6) Fields Registry**, **без `Show on map`**, чисто під MVP.

---

## 6. Fields Registry (FINAL, MVP)

### Призначення

Реєстр футбольних полів. Дозволяє зберігати та керувати полями, які використовуються для матчів, і швидко підставляти поле під час створення матчу.

---

## UI

### Header

* Заголовок: **Fields Registry**
* Кнопки:

  * **Back**
  * **+** (Add Field)

---

### Fields Snapshot (список полів)

Поля відображаються у вигляді вертикального списку карток.

#### Картка поля містить:

* **Фото поля** (опціонально)
* **Name** — назва поля
* **Type** — формат гри:

  * 5v5
  * 7v7
  * 11v11
* **Address**
* Дії:

  * **Use for match**
  * **Open**

---

## Дії користувача

### + Add Field

* Відкриває форму **Add / Edit Field** у режимі створення.

### Use for match

* Перехід до **Match Composer**
* Поле автоматично підставляється як вибране (prefill field).

### Open

* Відкриває форму **Add / Edit Field** у режимі редагування.

---

## Add / Edit Field Form

### UI елементи

#### Обовʼязкові поля

* **Name*** — назва поля
* **Address*** — адреса

#### Необовʼязкові поля

* **Type** — dropdown:

  * 5v5 / 7v7 / 11v11
* **Notes** — довільний текст
* **Lat / Lon** — координати
* **Photo** — фото поля

#### Кнопка

* **Save**

---

## Логіка

### Save

* Валідує обовʼязкові поля.
* Створює або оновлює поле в локальній БД.
* Повертає користувача до **Fields Registry**.

### Delete Field

* Видалення **заборонене**, якщо поле використовується у:

  * Scheduled матчах
* У такому випадку дія недоступна або показується повідомлення.

---

## Дані (MVP)

```json
field {
  id,
  name,
  address,
  type,        // 5v5 / 7v7 / 11v11
  notes,
  lat,
  lon,
  photoUri
}
```

---

## Примітки

* Реєстрація відсутня.
* Усі дані зберігаються локально.

---

## 7. Availability Grid

### Призначення

Екран планування, який дозволяє швидко побачити зайнятість команд і полів у часі та виявити конфлікти.
Це **не список матчів**, а візуальний планувальник (календар), орієнтований на організацію тижня або місяця.

---

## Навігація на екран

* **Hub → Quick Bar → Availability**
* **Match Composer → Check conflicts** (опціональний перехід)

---

## UI

### Header

* Заголовок: **Availability**
* Кнопка **Back**

---

### Top selectors (фільтри)

#### Period

* Значення:

  * **Week**
  * **Month**
* Впливає на формат відображення календаря.

#### Team filter

* Значення:

  * **Default team**
  * **Any team**
* Фільтрує матчі за участю команд.

#### Field filter

* Значення:

  * **All**
  * конкретне поле
* Фільтрує матчі за полем.

---

### Main view

#### Календар

* Відображення у вигляді календарної сітки:

  * дні — по колонках
  * час — у вигляді блоків:

    * **Morning**
    * **Afternoon**
    * **Evening**
* Кожен тайм-слот має візуальний статус.

#### Стани слотів (Legend)

* **Green** — вільний слот
* **Gray** — зайнятий (без конфлікту)
* **Red** — виявлений конфлікт

---

### Відображення матчів

У зайнятих слотах відображаються картки матчів:

* **Team A vs Team B**
* **Field**
* **Status chip**:

  * Planned
  * Finished

---

## Дії користувача

### Tap на вільний слот

* Дія: **Create match here**
* Перехід до **Match Composer**
* Автоматично підставляються:

  * дата
  * час

---

### Tap на матч

* Перехід до **Match Center** для цього матчу.

---

### Resolve conflicts

* Кнопка відображається **лише якщо є конфлікти**.
* Відкриває список конфліктів:

  * **Team clash**
  * **Field clash**
* Для кожного конфлікту пропонується:

  * швидка зміна часу
  * або зміна поля (якщо доступно)

---

## Логіка

### Джерело даних

* Усі дані беруться з локальної БД `matches`.

---

### Правила конфліктів

#### Team clash

* Одна й та сама команда:

  * бере участь у двох матчах
  * з перетином часу
  * з урахуванням **буфера 10 хвилин** після матчу.

#### Field clash

* Одне й те саме поле:

  * зайняте двома матчами з перетином часу.

---

### Resolve conflicts

* Показує список виявлених конфліктів.
* Дозволяє швидко:

  * перенести матч у інший час
  * або змінити поле (якщо є альтернативи).

---

## Обмеження та примітки

* Усі розрахунки виконуються локально.
* Availability Grid замінює окремий екран зі списком матчів (Fixtures Board).
* Перегляд **історії матчів** доступний через:

  * **Stats**
  * або фільтр у Availability (Finished only, у майбутньому).

---

## 8. Match Composer (створення / редагування матчу)

### Призначення

Екран створення та редагування матчу. Дозволяє задати основні параметри матчу: час, поле, команди та нотатки, з перевіркою конфліктів.

---

## UI

### Header (App Bar)

* Заголовок: **Match Composer**
* Кнопки (у режимі редагування):

  * **Edit** (іконка олівця)
  * **Delete** (іконка кошика)
* Кнопка **Back**

---

## Форма матчу

### Title*

* Текстове поле
* Обовʼязкове
* Placeholder: `Enter name`
* Використовується як назва матчу (опціонально може бути автогенерована, але редагується вручну)

---

### Date & Time*

* Обовʼязкове поле
* Date & Time picker
* Placeholder: `Enter`

---

### Field*

* Dropdown зі списком полів
* Кнопка **Pick from Fields**

  * відкриває **Fields Registry**
  * після вибору поле підставляється у форму

---

### Team A*

* Dropdown зі списком команд
* За замовчуванням:

  * підставляється **Default team**, якщо задано в Settings

---

### Team B

* Dropdown зі списком команд
* Опціональне поле
* Може залишатись порожнім (TBD)

---

### Notes

* Багаторядкове текстове поле
* Опціональне

---

## Footer

* **Save** — основна дія
* **Cancel** — скасування змін та повернення назад

---

## Логіка

### Prefill

* Якщо задані:

  * default team
  * default field
    → вони автоматично підставляються при створенні матчу.

* Якщо перехід з:

  * **Availability Grid** → підставляються дата і час
  * **Fields Registry (Use for match)** → підставляється поле

---

### Save

* Валідує обовʼязкові поля:

  * Title
  * Date & Time
  * Field
  * Team A
* Зберігає матч у локальну БД.

---

### Перевірка конфліктів

Перед збереженням:

* виконується перевірка на конфлікти часу.

#### Якщо конфлікт знайдено:

* показується попередження:
  **“Time conflict detected”**
* Кнопки:

  * **Save anyway** — зберегти матч, ігноруючи конфлікт
  * **Adjust** — повернутись до редагування

---

### Delete (edit mode)

* Доступна лише у режимі редагування
* Видаляє матч з локальної БД
* Після видалення повертає на попередній екран

---

### Навігація після Save

* Якщо відкрито з **Hub** → повернення на Hub
* Якщо відкрито з **Availability Grid** → повернення на Availability

---

## Дані (MVP)

```json
match {
  id,
  title,
  dateTimeStart,
  dateTimeEnd,
  fieldId,
  teamAId,
  teamBId,
  notes,
  status // planned / finished
}
```

> `dateTimeEnd` може обчислюватись автоматично (тривалість + буфер), якщо потрібно для Availability.

---

## Обмеження та примітки

* Реєстрація відсутня
* Усі дані зберігаються локально
* Match Composer не показує склади та тактику (це робиться в Match Center / Lineup & Tactics)
* Конфлікти не блокують збереження, лише попереджають

---

## 9. Match Center (деталі матчу)

### Призначення

Центральний екран керування конкретним матчем. Дозволяє переглянути деталі матчу, швидко виконати основні дії та зафіксувати результат після завершення гри.

---

## UI

### Header

* Заголовок: **Match Center**
* Кнопка **Back**

---

### Match summary card

* Заголовок: **Team A vs Team B / TBD**
* Інформаційний рядок:

  * **Date & Time**
  * **Field**
  * **Status** (Planned / Finished)
* Фон: зображення стадіону (декоративне)

---

### Rosters preview

#### Team A block

* Badge команди
* Назва команди
* Info row: Date/Time • Field • Status
* Дії:

  * **Edit match**
  * **Join Team B**
  * **Mark finished**
  * **Open Lineup & Tactics**

#### Team B block

* Якщо Team B задана:

  * Badge
  * Назва команди
  * Info row: Date/Time • Field • Status
* Якщо Team B = TBD:

  * відображається заглушка `Team B / TBD`
* Дії аналогічні Team A

---

## Дії користувача

### Edit match

* Відкриває **Match Composer** у режимі редагування.

---

### Join Team B

* Доступна, якщо **Team B = TBD**
* Дозволяє вибрати команду зі списку існуючих
* Після вибору:

  * команда підставляється як **Team B**
  * матч оновлюється в локальній БД

---

### Open Lineup & Tactics

* Перехід до **Lineup & Tactics Board**
* Доступна:

  * лише для **upcoming / planned** матчів

---

### Mark finished

* Переводить матч у стан завершення
* Відкриває блок **Finalize Match**

---

## Finalize Match

### UI

* Вибір результату:

  * **Team A win**
  * **Team B win**
  * **Draw**
* Ввід рахунку (формат: `3–2`)
* Кнопки:

  * **Save**
  * **Cancel**

---

### Логіка Finalize

* **Save**

  * зберігає:

    * статус матчу = **Finished**
    * результат
    * рахунок
  * оновлює матч у локальній БД
  * повертає користувача до Match Center у режимі Finished

* **Cancel**

  * закриває Finalize без змін

---

## Логіка та правила

* Після завершення матчу:

  * **Edit match** може бути обмежений або недоступний (MVP: дозволений)
  * **Lineup & Tactics** доступний лише для перегляду
* Дані матчу використовуються у:

  * **Stats**
  * **Availability Grid**

---

## Дані (MVP)

```json
match {
  id,
  title,
  dateTimeStart,
  dateTimeEnd,
  fieldId,
  teamAId,
  teamBId,
  status,      // planned / finished
  result,      // A_win / B_win / draw
  score        // "3-2"
}
```

---

## Обмеження та примітки

* Усі дії виконуються локально
* Match Center — єдина точка, де матч переводиться у статус **Finished**
* Без аналітики або подій матчу (голи, заміни) — тільки фінальний результат

---


## 10. Stats

### Призначення

Екран зведеної статистики по командам, матчам і полям. Дозволяє швидко оцінити активність та історію без редагування даних.

---

## Загальна структура

* Екран містить **три вкладки**:

  * **Teams**
  * **Matches**
  * **Fields**
* Верхня панель:

  * кнопка **Back**
  * заголовок **Stats**

---

## Tab: Teams

### UI

* Вертикальний список команд.

#### Картка команди містить:

* **Badge** команди

* **Name**

* Статистика:

  * **Played** (кількість зіграних матчів)
  * **W / D / L** (перемоги / нічиї / поразки)
  * **Avg roster size** (середня кількість гравців у матчах)

* Іконка переходу (`>`)

---

### Логіка

* Дані обчислюються на основі **Finished матчів**.
* **Tap по команді**:

  * відкриває список матчів, відфільтрований по цій команді
  * (MVP: може вести на вкладку Matches з активним фільтром)

---

## Tab: Matches

### UI

* Верхній блок зі зведеними показниками:

  * **Created**
  * **Scheduled**
  * **Finished**
* Нижче — **mini chart** (стовпчикова діаграма):

  * агрегація матчів по місяцях
  * кольори:

    * Created
    * Scheduled
    * Finished

---

### Логіка

* Усі дані беруться з локальної БД `matches`.
* Статуси:

  * Created — матч створено
  * Scheduled — запланований
  * Finished — завершений
* Графік є **інформативним**, без взаємодії (MVP).

---

## Tab: Fields

### UI

* Вертикальний список полів.

#### Картка поля містить:

* **Name**
* Коротку інформацію:

  * Date / Time (останнє використання)
  * Status (якщо релевантно)
* Іконка переходу (`>`)

---

### Логіка

* Для кожного поля підраховується:

  * **кількість використань** у матчах

* Поля можуть бути відсортовані:

  * за кількістю використань (Top 3 — логічний дефолт)

* **Tap по полю**:

  * відкриває поле у **Fields Registry** (режим перегляду / редагування)

---

## Дані (MVP)

### Teams stats

```json
teamStats {
  teamId,
  played,
  wins,
  draws,
  losses,
  avgRosterSize
}
```

### Matches stats

```json
matchesStats {
  created,
  scheduled,
  finished,
  perMonth: []
}
```

### Fields stats

```json
fieldStats {
  fieldId,
  usageCount
}
```

---

## Обмеження та примітки

* Усі статистики обчислюються **локально**.
* Stats — **read-only екран** (жодного редагування).
* Немає детальної аналітики подій матчу (голи, хвилини тощо).
* Екран призначений для швидкого огляду, не для глибокого аналізу.

---

# 11) Lineup & Tactics Board — FINAL MVP

## 🎯 Призначення

Єдиний робочий екран для **конкретного матчу**, де користувач може:

* сформувати стартовий склад
* зафіксувати базові тактичні налаштування
* організувати явку та поділ вартості поля
* зберегти все в контексті одного матчу (fixtureId)

Екран **не існує сам по собі**, він завжди відкривається з матчу.

---

## Як потрапляємо на екран

* **Match Center → Open Lineup & Tactics**
* (опціонально) Hub → кнопка Lineup, якщо є upcoming матч

---

## Загальна структура (для всіх вкладок)

### Header (постійний)

* Title: `{Team A} vs {Team B or TBD} — Match Board`
* Subline: `Kick-off: {date · time} · Field: {field name}`
* Actions:

  * **[Save]** — зберігає всі зміни (Lineup + Tactics + Logistics)
  * **[Back]** — повернення назад

#### Поведінка Save / Back

* Якщо є незбережені зміни → діалог:

  * `Discard changes?`
  * `[Keep editing] / [Discard]`

---

## Tabs

* **Lineup**
* **Tactics**
* **Logistics**

---

# Tab 1) Lineup

## 🎯 Ціль

Сформувати стартовий склад (XI) для матчу.

---

## UI

### Team Switcher

* Перемикач: **Team A | Team B**
* Якщо Team B = TBD:

  * вкладка Team B disabled
  * підказка: `Team B not assigned`

---

### Formation

* Dropdown:

  * 4-4-2
  * 4-3-3
  * 3-5-2
  * 5-3-2
* Note: `Slots update with formation`

---

### Pitch Board

* Вид зверху
* Фіксовані слоти:

  * GK / DF / MF / FW
* Кількість слотів визначається формацією
* Кожен слот:

  * tap → вибір гравця
  * tap на заповнений слот → Clear

❗️Drag & Drop **НЕ використовується в MVP**

---

### Available players

* Горизонтальний список “Available”
* Показує гравців, які ще не в Lineup
* Tap на гравця:

  * якщо є активний слот → призначає його
* Пошук по імені (optional)

---

### Кнопки

* **[Auto-arrange]**
* **[Clear lineup]**

---

## Player (в Lineup)

* Відображається:

  * імʼя / номер
  * POS (GK / DF / MF / FW)
  * колір форми (home / away)
* Long press / tap:

  * `Set / Remove Captain`

---

## Логіка

* Один гравець може бути тільки в одному слоті
* Captain — це просто прапорець у playerId
* Auto-arrange (MVP):

  1. читає вимоги формації
  2. підставляє гравців за позиціями
  3. якщо не вистачає — добирає універсальних
  4. Captain не скидається

### Валідації (мʼякі)

* Немає GK → warning `No goalkeeper assigned`
* Не всі слоти заповнені → warning `Lineup incomplete`
* Зберігати **дозволено**

---

# Tab 2) Tactics

## 🎯 Ціль

Зафіксувати прості тактичні налаштування та нотатки.

---

## UI

### Style controls

* Pressing:

  * Low / Medium / High
* Width:

  * Narrow / Balanced / Wide
* Build-up:

  * Direct / Mixed / Short

---

### Set Pieces Notes

* Corners (textarea)
* Free-kicks (textarea)

---

### Kit clash (MVP — автоматично)

* **Без UI**
* Логіка:

  * якщо homeColor Team A ≈ homeColor Team B →

    * Team B автоматично використовує Away kit
* Ніяких кнопок, статусів чи повідомлень

---

### Quick Match Notes

* Key matchups (multiline)
* Do / Don’t list:

  * чекбокси
  * кнопка **+ Add**
  * можливість видаляти пункт

---

## Логіка

* Все зберігається в:

```json
tactics {
  pressing,
  width,
  buildUp,
  corners,
  freeKicks,
  notes,
  doDont[]
}
```

---

# Tab 3) Logistics

## 🎯 Ціль

Організувати явку, витрати та підготувати короткий match sheet.

---

## UI

### Attendance

* Список гравців команди
* Checkbox `Coming`
* Лічильник:

  * `Confirmed: N / Total: M`

---

### Fee Split

* Input: `Pitch fee (total)`
* Split mode:

  * Confirmed only
  * All roster
* Output:

  * `Per player: $X.XX`
* Note: `This is a helper calculation.`

---

### Match Sheet Notes

* Meet time (time picker, optional)
* What to bring (chips):

  * Bibs
  * Water
  * Ball
  * Cash
* Crew notes (multiline)

---

### Кнопки

* **[Copy summary]** — копіює текст у буфер
* **[Back]**

---

## Логіка

* Attendance:

```json
attendance[fixtureId][teamId][playerId] = true/false
```

* Fee split:

  * confirmed → confirmed count
  * all → roster size
  * якщо 0 → показує “—”
* Copy summary формує:

  * матч
  * дата/час
  * поле
  * confirmed
  * fee per player
  * нотатки

---

## ✅ Що ВАЖЛИВО

* Ніяких платежів
* Ніяких інтеграцій
* Все локально
* Це **органайзер**, не симулятор і не менеджер клубу

---

# 12) Settings — FINAL MVP

## 🎯 Призначення

Екран базових налаштувань додатку:

* дефолтні значення
* локальні нагадування
* довідкова інформація
* повний скидання даних

Усі налаштування зберігаються **локально**, без акаунтів.

---

## UI

### Header

* Title: **Settings**
* Back

---

## Sections

### Defaults

* **Default team**

  * tap → відкриває список команд
  * вибір з існуючих
  * використовується для:

    * prefill у Match Composer
    * Availability
    * Lineup & Tactics (Team A за замовчуванням)

---

### Notifications

* **Local match reminders**

  * toggle ON / OFF
  * default: **ON**
  * працює через локальні нотифікації
  * нагадує про матч перед початком (фіксований offset, напр. 30 хв)

---

### About

* **Version**

  * показує версію додатку (read-only)
* **Privacy**

  * відкриває екран / модал з privacy text
  * статичний текст, без зовнішніх лінків (MVP)

---

## Actions

### Clear all data

* Кнопка: **Clear all data**
* Стиль: destructive (red)

#### Поведінка

1. Tap → confirm dialog:

   * `This will delete all local data. Continue?`
   * `[Cancel] / [Delete]`
2. Confirm:

   * повне очищення локальної БД
   * скидання всіх налаштувань
   * редірект на **Onboarding**

---

## Логіка

* Default team → `Settings.default_team_id`
* Notifications → `Settings.local_reminders_enabled`
* Clear all data:

  * wipe local storage / DB
  * app state = first launch

---

## Обмеження MVP

* ❌ Немає акаунтів
* ❌ Немає cloud sync
* ❌ Немає кастомних notification time
* ❌ Немає legal links

---

## ✅ Підсумок

Settings у MVP:

* мінімальний
* зрозумілий
* без прихованої логіки
* повністю відповідає офлайн-філософії FieldCaptain

---
